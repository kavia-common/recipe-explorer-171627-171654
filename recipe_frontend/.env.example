# Base URL for the backend API consumed by the Recipe Explorer frontend.
# If not set, the app will default to http://localhost:4000 and operate in mock mode.
REACT_APP_API_BASE_URL=http://localhost:4000

# Optional: Force API mock mode (overrides auto-detection).
# When true, API calls will return mock data without performing network requests.
# REACT_APP_API_MOCK=false
